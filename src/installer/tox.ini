[tox]
envlist = py36
skipsdist = true

[testenv]
usedevelop = true
deps =
    pytest
    six
    SQLAlchemy
    -e{toxinidir}/../core
commands = pytest --basetemp={envtmpdir} --capture=no --verbose {posargs}

`compute.json` is a sample software profile definition.

Use the following command-line to create it:

```shell
create-software-profile --name Compute --template compute.json --no-os-media-required
```
